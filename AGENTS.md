# Fiber agent notes

- Start here: `make help` and [docs/development.md](docs/development.md).
- Naming: `.cursor/rules/naming.mdc` — prefix is `fiber` / `FIBER_*`.
- DX: `.cursor/rules/dx.mdc` — use Make + `scripts/dev-env.sh`.
- Roadmap (later SCM/Vault/cloud agents): [docs/roadmap.md](docs/roadmap.md).
